class StackDemo {
    int top = -1;
    int[] a = new int[5];

    void push(int x) {
        if(top == 4) System.out.println("Full");
        else a[++top] = x;
    }

    int pop() {
        if(top == -1) return -1;
        return a[top--];
    }

    public static void main(String[] args) {
        StackDemo s = new StackDemo();
        s.push(10);
        s.push(20);
        System.out.println(s.pop());
    }
}
